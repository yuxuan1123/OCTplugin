# OCTplugin 未完成任务列表

> 生成时间：2026-09-15
> 参考：OCTools 插件逐步迁移至 Electron + Web UI 架构(D:\Project\PythonProject\OCTools\plugins)

---

## 一、插件迁移（主任务）

按 OCTools 目录顺序，home 之后还剩以下 6 个插件待迁移：

| # | 插件名 | 源目录 | 状态 | 备注 |
|---|--------|--------|------|------|
| 3 | **取色器** `color_pick` | `D:\Project\PythonProject\OCTools\plugins\取色器` | 未开始 | 阶段 3 |
| 4 | **md**（Markdown 预览/编辑） | `D:\Project\PythonProject\OCTools\plugins\md` | 未开始 | 阶段 4 |
| 5 | **目录树** `tree` | `D:\Project\PythonProject\OCTools\plugins\目录树` | 未开始 | 阶段 5 |
| 6 | **转换** `conversion` | `D:\Project\PythonProject\OCTools\plugins\转换` | 未开始 | 阶段 6 |
| 7 | **合并** `merge` | `D:\Project\PythonProject\OCTools\plugins\合并` | 未开始 | 阶段 7 |
| 8 | **翻译** `translation` | `D:\Project\PythonProject\OCTools\plugins\翻译` | 未开始 | 阶段 8 |

每个插件的迁移模板（已由 home 验证可用）：
1. 创建 `plugins/<id>/manifest.json`
2. 复制并重写后端 `main.py`（PySide6 → stdin/stdout JSON-RPC，去除 UI 依赖）
3. 创建 `plugins/<id>/ui/index.html` + `app.js`（iframe 网页 UI）
4. 主题统一使用 `plugin.css`（国风墨水：宣纸 `#f3ecdd`、墨黑 `#1e1b17`、暗金 `#a8875a`）
5. 宿主自动装配 iframe，内核 `/plugin/<id>/*` 静态路由 + WS 直连鉴权

---

## 二、home 插件已知待完善

- [ ] Hero 时钟倒计时字号：当前代码已根据 `is_countdown` 切换 46px / 40px，可在小屏再压缩一档
- [ ] 打卡 Toast 反馈：当前仅靠 `renderClock` 刷新，host 侧无 toast 组件，可补一个轻量 toast
- [ ] home.bat / net.ps1 缺失时的降级提示：后端返回 error 了，但前端可在 UI 上给更明确的"请放入同级目录"引导
- [ ] 网络加速滑块 30s 保持期倒计时展示：当前仅有文字提示，可加一个小型倒计时进度条

---

## 三、插件迁移避坑指南（基于 home 实际踩坑总结）

> 目标：写插件时一次到位，避免反复改 host 或主题。

### 3.1 高度 / 滚动条问题（最容易踩、坑最深）

不同插件的页面高度策略不同，**先定策略、再写样式**，否则滚动条会"只占一半/只在局部"。

- **策略 A —— 插件内部滚动（推荐给纵长页 / 数据列表）**
  - host 侧给 iframe `height:100%`，`.panel` 用 flex 铺满右侧。
  - 插件自身：
    ```css
    html{ height:100%; overflow:hidden; }
    body{ height:100%; box-sizing:border-box; overflow-y:auto; }
    ```
  - 滚动条就在插件内部，轨道 = 整块右侧高度，永远是满高一条。
- **策略 B —— 插件自适应内容高度（推荐给短页 / 落地页）**
  - host 用 `ResizeObserver` + `MutationObserver` 把 iframe 高度实时量成 `body.scrollHeight`，让 iframe 随内容长高，滚动交给 host 的 `.main`。
  - **注意**：内容长高/切卡时要触发重新量高，否则停在初始值。

**关键坑**：
- iframe 高度"断链"：`height:100%` 只对需要有确定高度的父链才生效；`.panel` 若未设高度，问100% 会回退成 auto，内容被裁掉 → 表现为"内容只显示顶部一点"。
- **插件的 `body.scrollHeight` 必须在面板可见时量**（面板初始 `display:none` 时量出 0，会被钉死在初始高度）。`ResizeObserver` 只盯 `overflow` 变化，纯文字/无图内容撑高时需配合 `requestAnimationFrame` + 面板显隐后再量一次。
- **`.main` 本身要能填满**：若宿主 body/`.main` 没有 `height:100%`，整个右区就没到窗口底部，插件再满也无济于事。
- 判断标准：**滚动条轨道是否是右区一整条满高**。是 → 高度链通了；不是 → 优先查 `.panel → iframe → html → body` 这条高度链上有没有断点。

### 3.2 固定像素高度 iframe 的坑
- 内联 `height:560px` 这类**固定高度优先级最高**，会盖掉一切 `height:100%`，直接表现为"缩放到多大都只显示一小条"。能不用内联 height 就不用；要保底请用 `min-height` 而不是 `height`。

### 3.3 无边框窗口（host 侧已迁移为 `frame:false`）
- host 窗口已无系统标题栏，顶部是自绘 header。
- **`.panel` 插件区域务必避开 header 高度**：header 设了 `-webkit-app-region:drag`，插件 iframe 不应覆盖到 header，否则无法拖动/窗口控制失灵。
- 插件页面若自身带顶部工具条，不要把它做成 `position:fixed` 顶到窗口最上，会被 header 遮住。

### 3.4 宿主通信（postMessage → 内核 WS 直连）
- UI 所有调用统一走：
  ```
  postMessage → 内核 WS → 'plugin.call'
  ```
- **`plugin.call` 的返回要解包 `result` 字段**（结构是 `{ok, result}`），响应在 `result` 里，不是直接可用。
- 动态数据（列表/状态）在页面**可见时再拉**，隐藏时拉可能拿到 0/空并缓成旧态。

### 3.5 国风墨水主题
- 只允许用 `resources/theme/plugin.css` 的 token：宣纸 `#f3ecdd`、墨黑 `#1e1b17`、暗金按钮 `#a8875a`、卡片**不**用蓝底、代码块 `#eae1d1`。
- 按钮主色用暗金渐变（`linear-gradient(135deg,#b9965f,#8f6f3d)`），不要用绿。
- 背景装饰（粒子/远山/山水长图）用 `position:fixed` + `z-index:-1` 铺满 iframe，能自动填满、不留白（配合 3.1 策略 A最佳）。

### 3.6 前端行为与后端状态解耦（避免"卡在中间态"）
- 一切开关/toggle 类组件（打卡、网络加速等）要**乐观更新 + 定时回读兜底**：先展示目标态，再定期 `rpc('state')` 校正，防止"正在开启…"卡住不动。
- 时间敏感逻辑（如"未到 18:00 禁止打卡"）在前端用 `setInterval` 定时重渲染，否则会卡在旧状态、跨过时间点后不自动放开。

### 3.7 home 迁移模板可复用点（新插件照抄即可）
- 高度用 3.1 的策略二选一；卡片三行布局用 `flex-direction:column`，不要把卡片用 `grid-template-columns: repeat(auto-fit, minmax(...))`（会不稳定、导致某些卡独占一行、某些并排）。

### 3.8 依赖隔离 & 解释器：看准内核的 rootDir（conversion 迁移最大坑）
- **内核的 rootDir = 内核二进制所在目录的上一级（项目根）**，不是 `Dir(os.Args[0])` 本身。项目根定位在 `kernel/cmd/kerneld/main.go`，`plugins / store / resources / deps` 都挂它下面。
- **插件的隔离 venv 固定位置是 `项目根/deps/<id>/.venv`**，不要手建到别的目录（如把 venv 建在 `项目根/deps` 之下是对的，但若把 `requirements` 装到 `kernel/deps` 或别处就是孤儿，内核不会用）。
- **插件启动用的解释器由 venv 是否存在决定**：`.venv` 存在 → 用 venv python；不存在 → 内核回退到**无依赖的系统 python** → 任何第三方库（`docx`/`pypandoc` 等）`ModuleNotFoundError`，表现为插件反复 crash+paused。
- **依赖是否"已安装"只看 venv 目录在不在**，不会自动重装；要让插件用上新加的 requirements，需要触发 `deps.install`（宿主"安装依赖"按钮走 `deps.install` RPC），或确保 venv 内已装齐。
- **系统目前不会在插件启动时自动 `uv pip install -r requirements.txt`**。因此新增依赖后，要么手动触发安装，要么重启前确认 venv 已就位，别指望启动自装。

### 3.9 后端 1:1 复刻：别写成简化 MVP
- 复刻 OCTools 类企业级插件时，格式/引擎/调度要分层完整移植：`core/formats`（能力位→Format→registry→queries 单一真相源）、`core/engines`（原子引擎）、`services/conversion`（direct_table 直达 + planner 可达性 + pipeline 统一入口 + star 星型 BFS 寻路 + batch + cross_category）、`config`（纯 dataclass，去 PySide）。
- 跨类转换走星型枢纽（doc→md、table→json、video→mp4、audio→wav、位图→jpg、向量→svg），`html→docx = ['html','md','docx']` 这类用 BFS 找路径，不是硬编码。
- **重依赖引擎（numpy/PySide 等）不要放进启动 import 链**：`core/engines/__init__.py` 只 import 转换所需的轻引擎，其余（audio_capture/translation/screenshot）用 `__getattr__` 延迟加载，否则插件一启动就崩。

### 3.10 前端"选中串"同步：输出路径与目标格式必须同源
- **凡是"自动推算"输出值的地方，务必避免"有值就不再重算"的守卫**（如 `if (out.value.trim()) return;`）。一旦先选了 A 格式把输出框填成 `A`，再换 B 格式时该守卫会跳过重算，导致"显示选 B，实际按 A 转"。
- 正确做法：加一个"是否用户手动填写"的标记（`outManual`），目标/源变化时只要不是手动就**重新推算**；用户手动改过才不再覆盖。
- 后端 `direct_table` 会**优先按传入的 `output` 路径的扩展名决定转成什么**，所以前端若不传 output 应让后端按 `target` 推导；一旦传了带旧扩展名的 output，target 就被忽略。

### 3.11 前后端并行数组（labels/values）若同源可放心，但仍建议以 id 为锚
- 若后端用同一份列表同时生成 `values` 与 `labels`（`labels=[display(f) for f in fmts]`），顺序一定一致，可放心按下标对应使用。
- 但为了排查方便，前端每次渲染图层时**以 `data-fmt`（格式 id）为锚**渲染 `textContent`，避免将来某处排序/过滤变化导致"显示 XLSX 实为 CSV"这类错位。

### 3.12 静态推理和运行时矛盾时，先加日志再下结论
- 本次 `selectDst`/`renderChips` 代码读起来都对、但运行时"点 XLSX 却转 CSV"，最终根因在**另一处**（输出路径自动推算的守卫），说明：**能用运行时证据就别只靠静态读**（`console.log` 抓 `dataset.fmt`、`dstFmt`、实际传给后端的 `params.output`），同时排查"目标扩展名来自哪个变量、是否被旧缓存值污染"。

### 3.13 前端启动必须等 `oct.ready`，不要提前调后端（md 实际踩坑）
- **`window.OCT` 桥对象是同步就存在的，但 WS 未必连上**。app.js 在末尾同步给 `window.OCT` 赋值，此时 `ws.readyState !== 1`，任何 rpc 都会 `reject("内核未连接")`。
- 表现为：**黄色插件首次打开，默认路径/列表空白，必须手动点一次刷新才出现**（首次 `md.list` 失败后没有任何重试）。
- 统一规范：所有插件启动只需一条
  ```js
  window.addEventListener("oct.ready", () => { init(); });
  ```
- **不要**写 `if (window.OCT) { init(); return; } setTimeout(start, 60);` 这类提前分支——它是"先试、失败、静默放弃"的死路径，只会制造首次空白。即使加了 `oct.ready` 兜底能修好，死路径也应删掉，保持单一、直白的启动时序。
- 同理：只有**用户点击才触发的后端调用**（tree 的目录扫描等）不涉及启动时序，无需 `oct.ready` 守卫。

### 3.14 共享运行时/工具放项目根，配置走全局 store，别埋进单个插件
- **LibreOffice 这类要被多插件复用的重工具，安装在 `项目根/tools/<name>/`**（如 `tools/libreoffice/program/soffice.exe`），不要塞进某个插件的 `.runtime/<id>/`——否则迁走/删插件会把所有调用它的人都坑掉。
- **共享配置写全局 `项目根/store/<name>.json`**，归属宿主"设置面板"统一管理（选"已安装目录 / 本地 MSI / 自动下载"，配进度条），**插件代码内不做首次自动下载**；代码只做"只探测已存在"（`find_bin(auto=False)`），找不到就抛错并指向设置面板。
- 关键坑：**搬 LibreOffice 安装根时肢体必须完整**——安装根含 `program/`、`share/`、`help/`、`Fonts` 等同级目录，只搬 `program/` 会因缺 `share/` 在启动时以 MSVC C++ 异常（退出码 `0xE06D7363`）崩溃。


