# OCTplugin 进程说明

> 从「进程如何工作」的角度讲清：谁启动了谁、怎么通信、进程是否常开、有无闲时休眠。
> 覆盖：内核(kerneld)、home 插件、md 插件、以及它们之间的进程关系。
> 语言：Go（内核）+ Electron/Node（宿主）+ Python（插件后端，纯标准库）。

---

## 1. 进程全景

运行一个 OCTplugin 时，机器上实际存在 **4 类进程**：

```
┌─────────────────────────────────────────────────────────────────────┐
│ ① Electron 宿主（Node.js）                      host/main.js          │
│    └─ 渲染进程：主窗口 file://… → 每个插件一个 iframe(web 页面)         │
│    └─ 渲染进程：md 编辑器可再开一个独立 BrowserWindow                 │
└──────────────┬──────────────────────────────────────────────────────┘
               │  spawn() 启动 + 从 stdout 读 auth 行 + WS 连接
               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ ② Go 内核 daemon                              kernel/kerneld.exe      │
│    · HTTP/WS 服务 127.0.0.1:随机端口                                  │
│    · 扫描 plugins/* 并启动所有插件                                    │
└──────────────┬──────────────────────────────────────────────────────┘
               │ StartAll()：每个插件一个子进程，stdin/stdout 管道 JSON-RPC
               ▼
┌─────────────────────────────────────────────────────────────────────┐
│ ③ 插件后端进程（Python / Node，逐插件隔离）                          │
│    · home  → plugins/home/main.py（无界面，纯 stdin/stdout 服务）    │
│    · md    → plugins/md/main.py（同上）                              │
└──────────────┬──────────────────────────────────────────────────────┘
               │ home 在“工作模式/网络加速”时再派生一次性短命进程
               ▼
┌──────────────┴──────────────────────────────────────────────────────┐
│ ④ 一次性外壳进程（短命，跑完即走）                                   │
│    · cmd /c home.bat   （工作模式脚本）                               │
│    · powershell net.ps1（网络加速开关脚本）                           │
│    · powershell 探测命令（网络加速状态轮询）                          │
└─────────────────────────────────────────────────────────────────────┘
```

对应代码入口：

| 进程 | 代码 | 说明 |
|---|---|---|
| Electron 宿主 | `host/main.js` | `startKernel()` 拉起内核，`createWindow()` 建主窗口 |
| Go 内核 | `kernel/cmd/kerneld/main.go` | 内核唯一入口 |
| 插件后端 | `plugins/<id>/main.py` | 每个插件一个，逐插件 venv/node 隔离 |
| 插件前端 UI | `plugins/<id>/ui/index.html` + `app.js` | 跑在宿主 iframe 里的网页，非独立进程 |

---

## 2. 内核(event kerneld.exe)怎么工作

内核是一个**常驻的 Go daemon**，启动流程（`main.go`）：

1. 定位插件目录 `../plugins`、资源目录 `resources`、配置目录 `store`。
2. 读授权持久化 `store/perms.json`（权限门 `perms.Gate`）。
3. 用 `uv` 定位**托管 Python 3.12**（`only-managed`，不依赖系统 Python）；依赖隔离由 `deps.Installer` 管理（Python→`deps/<id>/.venv`，Node→插件目录内 `node_modules`）。
4. 生成一次性随机 `token`，监听 `127.0.0.1:0`（随机端口，仅本机）。
5. `manager.StartAll()`：**扫描并启动所有插件**（见 §4）。
6. `WireEvents()`：把插件事件广播出口接到宿主连接。
7. 在 **stdout 打印一行 auth**：`{"auth":{"port":<端口>,"token":<token>}}`，然后 `select{}` 永久阻塞（内核不退出）。

内核对外只做两件事：

- **资源/静态页面服务（普通 HTTP）**
  - `/res/*` → `resources/` 共享资源（带目录穿越防护）；
  - `/plugin/<id>/<rel>` → 插件自带 UI 页面（`servePluginUI`，同样防穿越）；
  - `/favicon.ico` → 返回应用 logo，避免新窗口取图标时报 400/404；
  - 其它普通 HTTP → 404（绝不进 WebSocket 升级逻辑）。
- **WebSocket 服务（内核与宿主/插件 UI 的通信通道）**

### WS 多连接模型
内核允许多条 WS **并发共存、互不互踢**：

- 宿主主连接 `client=octplugin-host`（唯一事件广播目标）；
- 每个插件 iframe 一条连接 `client=<pluginId>`；
- md 编辑器新窗口一条连接 `client=md`（自连）。
- 每条连接首条消息必须是 `kernel.hello`（带 token），否则关闭。

### 协议分发
连接建立后，内核根据方法名分发：`plugin.list / plugin.call / plugin.details / plugin.restart`、`perms.*`、`deps.*`、`registry.*`、`command.*`、`kernel.ping`。

---

## 3. 各进程之间怎么通信

### 3.1 Electron 宿主 ↔ Go 内核：管道 + WS
1. `host/main.js` 用 `spawn(kerneld.exe)` 派生内核进程；
2. 从内核 stdout 读那行 **auth**，拿到 `port` + `token`；
3. 宿主连 `ws://127.0.0.1:port`，发 `kernel.hello {client:"octplugin-host", token}`；
4. 成功后宿主把 `{port, token}` 广播给渲染进程（`kernel:ready`）。

### 3.2 宿主渲染进程 ↔ 内核：WS（经 iframe 中转）
`host/index.html` 收到 `kernel:ready` 后，为每个插件建 iframe：

- `frame.src = 内核 /plugin/<id>/ui/index.html`；
- 用 `postMessage({type:"oct.hello", port, token, pluginId})` 把内核地址交给 iframe。

`plugins/<id>/ui/app.js`（iframe 内）收到 `oct.hello` 后：

- `new WebSocket("ws://127.0.0.1:port")` → `kernel.hello {client:pluginId, token}`；
- 成功后暴露 `window.OCT.callPlugin(pluginId, method, params)`。

### 3.3 插件 UI ↔ 插件后端：经内核中转（plugin.call）

```
iframe(前端)  plugin.call(md.list)            内核                插件后端(main.py)
   │                │  WS                     │   stdin写一行 JSON-RPC   │
   │──────────────────────────►  plugin.call ───────────────────────────►│
   │                                      读句 stdin循环 (: for line in sys.stdin)
   │                                      处理 handle(method, params)
   │ ◄──────────────────────────  stdout写一行 result ◄──────────────────│
   │                ◄  WS 返回                           │
```

- 插件后端就是一个**读 stdin、写 stdout 的 JSON-RPC 行服务**；
- 内核把前端请求转发给它，再把它的 stdout 响应回给前端。
- **注意**：内核读插件 stdout 单行上限 **8 MB**，超大输出（如整屏截图 base64）会撑爆缓冲杀死插件进程——大文件要落盘+HTTP 传输，别走 WS。

### 3.4 插件后端 → 内核 → 宿主：请求型（插件主动发起）
插件除了响应，也能主动向内核发请求（`readLoop` 里看到带 `method` 的行即认为是请求）：
- `gate.file_read` / `gate.file_exists`：受权限门保护的受限服务；
- `registry.call`：插件 A 跨插件调用插件 B 的共享函数；
- `event.emit`：插件广播事件给宿主（走宿主连接）。

### 3.5 md 编辑器“独立新窗口”链路
1. md 管理页若右键/双击/点「新窗口编辑」，iframe 向宿主 `postMessage({type:"oct.openPluginWindow", path:…})`；
2. 宿主 `index.html` 拼内核地址 → `ipcRenderer.invoke("win:openPluginWindow")`；
3. `host/main.js` 创建一个 `BrowserWindow`，`loadURL(/plugin/md/ui/editor.html?mdEditor=1&path=…&auth=…&kport=…)`；
4. 新窗口的 `app.js` 见 `?mdEditor=1` 时**不再等宿主 postMessage**，直接从 query 里读 `kport/auth` 自连内核（`client=md`）→ 与普通 iframe 一样用 `plugin.call` 读文件。

---

## 4. 插件进程：常开吗？有闲时休眠吗？

### 4.1 启动时机
内核 `StartAll()` 在**内核启动时就把 plugins/ 下所有插件全部拉起**（MVP：全启动）：
- 每个插件 = `exec.Command(解释器, entry)`，`cwd=插件目录`、隐藏窗口；
- Python 解释器优先用该插件 venv（`deps/<id>/.venv`，无则托管 Python 3.12）；Node 插件用 node.exe。
- 注意：虽然 `manifest.json` 里有 `load_mode`（`always` / `lazy` / `auto_recycle`）字段，**当前内核实现统一按“全启动”处理，未真正区分**。

### 4.2 常开与保活（心跳）
- **插件进程常驻**：一旦被内核拉起，就常驻内存等待请求，不会因为“没用”而退出。
- **心跳**：内核每 **5 秒**给每个插件写一行 `ping`（`HeartbeatInterval=5s`）；插件读到 `ping` 回 `pong`。
- **进程消亡检测**：内核读插件 stdout 读到 **EOF** 即认为进程结束/崩溃（`markDead`），标记 `alive=false`、唤醒所有 pending 并报“插件崩溃”。当前**不会自动重启**，需要 `plugin.restart`。
- *（说明：代码里 `HeartbeatTimeout=3×5s=15s` 已定义，但当前用 stdin 写 ping 保活 + stdout EOF 判死，未实现“15s 无 pong 踢除”的强超时逻辑。）*

### 4.3 闲时休眠 / 回收
- **目前没有“闲时休眠”**：插件 7×24 常驻，空闲不睡、不用不回收。
- `load_mode=auto_recycle`（按需拉起 + 空闲自动回收）是**预留设计，尚未实现**。这是 future work：让不活跃插件在空闲若干秒后退出、用到时再懒启动，以省内存。

### 4.4 一次性短命子进程（例外）
插件后端会 `subprocess.Popen` 派生**短命外套进程**，用完即走，不属于常驻：
- home：`cmd /c home.bat`（工作模式）、`powershell net.ps1`（网络加速）、`powershell` 探测命令（加速状态）。
- 这些非内核管理，**不参与心跳**。

---

## 5. home 插件与内核的配合

home 后端（`plugins/home/main.py`）是**纯标准库的 stdin/stdout JSON-RPC 服务**，无任何 UI 依赖（严禁 PySide6）。

| 方法 | 作用 | 备注 |
|---|---|---|
| `state` | 聚合返回打卡状态 + 任务 + 今日 + 网络加速状态 | 前端主页 iframe 定时轮询 |
| `clockin` | 打卡，写入该时段时间 | 持久化到 `home_state.json` |
| `task.add / toggle / remove` | 今日任务增/勾/删 | 同文件持久化 |
| `workmode` | 静默跑 `home.bat` | `Popen(cmd /c home.bat)`，一次性 |
| `net.status` | 探测当前是否已加速 | 派生 powershell 查静态 IP 是否存在 |
| `net.toggle` | 执行 `net.ps1 -Action on/off` | 一次性 powershell |
| `ping` | 心跳应答 | 内核每 5s 发 |

前端主页 iframe 通过 `plugin.call("home","state",…)` 取状态并渲染；`.bat`/`.ps1` 缺失时后端返回显式 `{ok:false, error}`。

---

## 6. md 插件与内核的配合

md 后端（`plugins/md/main.py`）同样是纯标准库行服务。**特别点**：启动时把 `sys.stdin/stdout` 强制 `reconfigure(encoding="utf-8")`，否则 Windows 按 cp936 解码 UTF-8 的中文路径，中文文件名会找不到。

| 方法 | 作用 |
|---|---|
| `md.list` | 扫描工作目录 `.md` 与指向 `.md` 的 `.lnk`，返回展示字段（标题/大小/时间） |
| `md.read / save / create / delete / rename` | 读、写、新建（自动去重加 `(2)`）、删除（lnk 只删本体）、同目录重命名 |
| `md.setDir / getDir` | 切换 md 工作目录（持久化到 `.cache/config.json`） |
| `ping` | 心跳应答 |

UI 分两种形态，都走同一个后端：

- **管理页**（iframe，/plugin/md/ui/index.html）：列表 + 快速预览 + 搜索；
- **编辑器**（独立的 BrowserWindow，见 §3.5）：由管理页请求宿主开新窗口，`editor.html` 经 query 里的 `kport/auth` 自连内核，再用 `plugin.call("md","md.read")` 加载文件。

---

## 7. 一次性总结

| 问题 | 结论 |
|---|---|
| 内核常开吗？ | 是，`select{}` 永久阻塞，宿主 quit 时才随宿主退出 |
| 插件常开吗？ | 分级启动：`always` 常驻；`prewarm/lazy` 按需 `GetOrStart` 拉起；`disabled` 不启 |
| 进程间通信 | 宿主↔内核=管道+WS；内核↔插件=stdin/stdout JSON-RPC 行；UI↔内核=WS `plugin.call`（懒启动自动先拉起） |
| 插件闲时休眠？ | **已支持**：`recycle.idle_recycle=true` 且非 `always` 时，空闲 `max_idle_ms` 后优雅回收为 `idle`，按需再拉起 |
| 插件每 5s 干嘛 | 收到内核的 `ping` 心跳；超 `heartbeat.timeout_ms` 无响应会被踢除 |
| 插件崩溃会重启吗？ | 会（`resilience.auto_restart=true` 默认）：指数退避 + 窗口内限次；正常退出码(`crash_exit_codes`)不重启 |
| 哪些进程是短命的一次性的？ | home 派生的 `home.bat` / `net.ps1` / powershell 探针 |

---

## 8. 待办 / 说明

- `load_mode`（`lazy` / `auto_recycle`）字段已在 manifest 与 Plugin 结构里预留，但**尚未落地**：目前等同“always 全启动、常驻、无闲时回收”。
- 若未来要省内存，可在内核加：每日空闲 N 分钟自动回收 + 按需懒拉起（`plugin.call` 时热启动）——这属于 §4.3 预留能力。