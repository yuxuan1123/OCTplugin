# 插件生成规定与注意点

> 汇总自 OCTplugin 各插件（home / md / tree / conversion / merge / color_pick / translation）实战经验。
> 目标：写插件时一次到位、风格统一、不返工。凡是"规定"即强制；"注意点"为高发坑位。

---

## 一、目录与命名

- 插件根 = `plugins/<id>/`，id 全小写、短横线分隔（如 `color_pick`、`conversion`）。
- **id 是稳定标识**（目录名即 id，内核/宿主插件序/默认页都依赖它）；改显示名只改 `manifest.json` 的 `name`，不改 id、不重命名目录。
- 结构约定：
  ```
  plugins/<id>/
    manifest.json
    main.py                 # 后端：异步 stdin/stdout JSON-RPC
    ui/
      index.html            # 管理页（宿主 iframe）
      editor.html(可选)     # 独立编辑器窗口
      app.js                # 桥客户端（始终复用模板）
      ui.js                 # 业务逻辑
      plugin.css(共享)      # 主题统一用 resources/theme/plugin.css
    core/ services/ config/ # 后端分层（复刻企业级插件时）
    requirements.txt
  ```

## 二、manifest 硬性要求

- **不得含 UTF-8 BOM**，否则内核 JSON 解析失败。
- `type` 用 `Python`（或 `Node`/`JS`），`entry` 指向后端入口。
- `load_mode`：`always` = 启动预启动；`lazy` = 页面首开才 `GetOrStart`。
- 所有后端方法都登记进 `functions`（method 统一 `插件前缀.方法名`，如 `md.list`）。
- 对应权限声进 `permissions`；勾选=允许、取消=撤销、实时生效、失败自动回滚。

## 三、前端初始化时序（核心规定）

- **一律等 `oct.ready` 再调后端**，只有一条：
  ```js
  window.addEventListener("oct.ready", () => { init(); });
  ```
- **禁止** `if (window.OCT) { init(); return; }` 提前分支——`window.OCT` 是 app.js 末尾同步赋值的，但 WS 未必连上，此时 rpc 会 `reject("内核未连接")`，首次打开就空白、且不重试。
- 仅**用户点击才触发**的后端调用（如 tree 扫描）不涉及启动时序，无需守卫。

## 四、桥接与 RPC 规定

- 页面跑在 iframe 里，由宿主 postMessage 给 `oct.hello`(port/token/pluginId)，app.js 据此直连内核 WS（`kernel.hello` 鉴权后 `dispatchEvent('oct.ready')`）。
- **所有后端调用走 `window.OCT.callPlugin(id, method, params)` → `plugin.call`**。
  - 千万不要把自定义方法名（如 `merge.formats`）当原始 WS 方法直接 `ws.send`——内核 dispatch 只认服务端方法（`plugin.call`/`plugin.list`…），不认自定义名，会走 `ErrMethodNotFound`。
- **透传/解包两层的坑**：
  - 外层 `plugin.call` 返回 `{ok, result}`；
  - 内层才是后端返回（可能又是一个 `{ok, result}` 或 `{ok, error}`）。
  - 统一在三处解包，别在业务代码里到处散。
- 独立窗口（编辑器）模式：直接读查询参数 `?mdEditor=1&kport=&auth=&path=` 自连内核，不走宿主 parent 握手。

## 五、主题与视觉（国风墨水）

- 只用 `resources/theme/plugin.css` 的 token：宣纸 `#f3ecdd`、墨黑 `#1e1b17`、暗金按钮 `#a8875a`、代码块 `#eae1d1`、卡片**不用蓝底**。
- 主按钮用暗金渐变 `linear-gradient(135deg,#b9965f,#8f6f3d)`，不用绿；危险操作用墨红系。
- 背景装饰（粒子/远山/山水长图）用 `position:fixed; z-index:-1` 铺满 iframe。

## 六、高度策略（先定策略再写样式）

- **策略 A（纵长页/数据列表）**：插件内滚动。
  ```css
  html{ height:100%; overflow:hidden; }
  body{ height:100%; box-sizing:border-box; overflow-y:auto; }
  ```
- **策略 B（短页/落地页）**：自适应内容高度，host 用 `ResizeObserver`+`MutationObserver` 把 iframe 高度实时量成 `body.scrollHeight`，滚动交给 `.main`。
- **必须是 `body` 的 `scrollHeight` 在面板可见时量**（隐藏时量出 0 会被钉死）。`.main` 本身也要能填满窗口。
- **禁用内联固定 `height:560px`**（优先级最高，会盖掉 `100%`，表现"缩放多大都只显示一小条"）；要保底用 `min-height`。
- 判断标准：滚动条轨道具 `.main` 一整条满高即高度链通畅。

## 七、……与后端交互的通用规定（插件面板布局）

- 插件面板用绝对定位填满 `.main`：`left:24px; right:24px; top:24px; bottom:24px`，`iframe height:100%`，不留白。
- 无边框窗口顶部是自绘 header，插件 iframe **不得覆盖 header**；插件自带顶栏不要 `position:fixed` 顶到最上。
- 主题/滚动都遵守 五、六。

## 八、后端分层（复刻企业级插件，别写成简化 MVP）

- 分层完整移植：`core/formats`(能力位→Format→registry→queries 单一真相源) → `core/engines`(原子引擎) → `services/<cap>`(direct_table / planner 可达性 / pipeline / star 星型 BFS)。
- 跨类转换走星型枢纽，路径用 BFS 推算而非硬编码（如 `html→docx=['html','md','docx']`）。
- **重依赖引擎不要进启动 import 链**：`core/engines/__init__.py` 只 import 轻引擎，重的用 `__getattr__` 延迟加载，否则插件一启动就崩。
- 插件进程内仅用标准库 + 自身 venv 依赖，避免第三方冲突；重工具（LibreOffice/ffmpeg）外置。

## 九、共享运行时 & 配置（多插件复用）

- **重工具装 `项目根/tools/<name>/`**（如 `tools/libreoffice/program/soffice.exe`），不要埋进单插件 `.runtime/<id>/`。
- **共享配置写 `项目根/store/<name>.json`**，归宿主"设置面板"管；**插件代码不自动下载/不自动安装**，只 `auto=False` 探测已存在，找不到抛错并指向设置面板。
- 搬工具**肢体必须完整**：如 LibreOffice 安装根含 `program/ share/ help/ Fonts` 同级目录，只搬 `program/` 会因缺 `share/` 以 MSVC C++ 异常（退出码 `0xE06D7363`）崩溃。

## 十、动态数据与时间敏感 UI

- 开关/toggle 类（打卡、网络加速）**乐观更新 + 定时回读兜底**（如 20s 定时重渲染/轮询），避免"卡在中间态"。
- 时间敏感逻辑（未到 18:00 禁打卡）前端 `setInterval` 定时重渲染，跨时点自动放开。
- 状态颜色由内核实时 `plugin.list` State 驱动，不做乐观 UI；前端订阅 `plugin.state` 广播 + 600ms 轮询兜底。
- 插件被禁用时后端返回 `disabled` 占位页，HTTP 请求不再触发 `GetOrStart`。

## 十一、输出路径与目标格式必须同源

- 自动推算输出值处，**禁止"有值就不重算"守卫**（`if (out.value.trim()) return;`）——先选 A 再换 B 会被旧值污染。
- 加"是否用户手动填写"标记（`outManual`），非手动则**总是重算**。
- 后端 `direct_table` 优先按 `output` 扩展名决定转什么，前端不传 output 时后端按 `target` 推导；传了旧扩展名 output 会忽略 target。

## 十二、运行性能与资源

- 长耗时操作要设超时（前端 rpc timeout `15s`；长任务如取色/格式加载 `timeoutMs:600000`）。
- 后台/子进程任务完成后**及时终止**，避免资源泄漏；同步子进程探测带超时（普通 15s，依赖就绪探测 120s），一个插件卡住只影响自己、不阻塞整体启动。
- 依赖仅为"venv 存在→判已安装"，不自动装；新增 requirements 后需主动触发 `deps.install`。

## 十三、运行时证据优先

- 静态读没错、运行时却不对时，**先加 `console.log` 抓运行时值**（`dataset.fmt`/`dstFmt`/传给后端的 `params.output`），再下结论，别只靠静态推理。
- 错误处理：异常要打印详细信息；API 可失败处**不要无条件 `return`** 吞掉错误，留 fallback。

## 十四、一致性检查清单（发版前自检）

- [ ] manifest 无 BOM，`name` 显示正确，方法都登记
- [ ] 前端启动只有 `oct.ready` 一条，无提前分支
- [ ] 所有后端调用走 `plugin.call` 并正确解两层包
- [ ] 主题只用共享 token，卡片无蓝底，主按钮暗金
- [ ] 高度策略明确：策略 A 或 B，无内联固定 height
- [ ] iframe 填满 `.main`，不覆盖 header
- [ ] 重工具在 `tools/`、共享配置在 `store/`，插件不自动下载
- [ ] 乐观更新 + 定时回读；时间敏感逻辑定时重渲染
- [ ] 输出路径/目标格式无"有值不重算"守卫
- [ ] 长任务设超时；子进程终止得体
- [ ] py_compile / node --check 通过