以下是针对“全启动策略”修改的**完整实施步骤清单**，按优先级和依赖关系排列。

---

### 第一阶段：数据结构与 manifest 定义

1. **定义 manifest 新增字段 schema**  
   - 在 `plugin/manifest.go` 中添加 `startup`、`heartbeat`、`recycle`、`resilience`、`limits`、`triggers` 结构体，与上文 JSON 结构一一对应。  
   - 为 `load_mode` 定义枚举：`always`、`prewarm`、`lazy`、`disabled`。  
   - 为 `triggers.on_command`、`on_ui_open`、`on_file_ext`、`on_event` 定义字符串数组。  
   - 所有字段提供合理的 Go 零值默认（如 `load_mode` 默认 `prewarm`，`auto_restart` 默认 `true`）。

2. **编写 manifest 校验函数**  
   - 校验 `load_mode` 是否为合法枚举值。  
   - 校验 `heartbeat.timeout_ms` ≥ `heartbeat.interval_ms`。  
   - 校验 `recycle.max_idle_ms` > 0 如果 `idle_recycle=true`。  
   - 校验 `resilience.backoff_base_ms` 和 `backoff_max_ms` 合理。  
   - 校验 `limits.stdout_line_bytes` ≤ 8MB（硬限制）。  
   - 校验 `triggers` 中引用的命令名、UI 页面 ID 是否在插件自己的声明中（可选，早期可跳过）。

3. **更新插件注册表存储结构**  
   - 在 `registry.Manifest` 中嵌入上述新字段。  
   - 确保旧 manifest 向后兼容（缺失字段自动填充默认值）。

---

### 第二阶段：内核 supervisor 改造

4. **实现分级启动分发器**  
   - 修改 `supervisor.StartAll()`：遍历所有已注册插件，按 `load_mode` 分组：
     - `always` → 立即调用 `SpawnAndInit(pluginID)`。
     - `prewarm` → 仅预创建进程池（可选）或注册到预热队列，但不 spawn。
     - `lazy` → 只注册到触发器映射表，不 spawn。
     - `disabled` → 跳过。
   - 依赖未安装的插件先标记 `deps_pending`，依赖安装完成后自动按原 `load_mode` 处理。

5. **实现懒启动（GetOrStart）**  
   - 新增 `supervisor.GetOrStart(pluginID)` 方法，返回 `(RunningInstance, error)`。
   - 逻辑：
     - 检查插件状态：`running` → 直接返回。
     - `starting` → 等待初始化完成（channel/cond），超时返回错误。
     - `stopped` 或 `idle` → 重新 spawn，走初始化流程。
     - `disabled` → 返回错误“插件已禁用”。
   - 所有入口（`plugin.call`、命令面板、UI 打开、文件匹配）统一调用 `GetOrStart`。

6. **实现心跳超时踢除**  
   - 每个插件实例维护 `lastPong time.Time`，goroutine 每 `heartbeat.interval_ms` 发 ping，收到 pong 更新 `lastPong`。
   - 另起一个 checker goroutine（每 1 秒检查一次），若 `now.Sub(lastPong) > heartbeat.timeout_ms`，则标记为 `unhealthy`，触发重启流程（走 resilience）。
   - 注意：ping/pong 走 stdin/stdout JSON-RPC，不影响正常业务请求。

7. **实现空闲回收**  
   - 每个插件实例维护 `lastActivity time.Time`，每次收到请求、事件、订阅时更新。
   - 如果 `recycle.idle_recycle=true`，启动一个定时器（间隔 10 秒），检查 `now.Sub(lastActivity) > recycle.max_idle_ms` 且当前无进行中的请求 → 触发优雅关闭：
     - 发送 `shutdown` 请求给插件（超时 `graceful_shutdown_ms`）。
     - 等待后 kill 进程。
     - 状态转为 `idle`（保留注册表，下次 GetOrStart 时重新 spawn）。
   - 插件有后台轮询（如定时任务）需在 manifest 中声明 `background_tasks=true`，内核据此跳过空闲回收。

8. **实现自动重启与退避**  
   - 在进程崩溃（EOF/exit code）或心跳超时踢除后，检查 `resilience.auto_restart`。
   - 维护每个插件的重启计数器（滑动窗口 `max_restarts_window_ms` 内）。
   - 若未超限：等待 `backoff_base_ms * 2^attempt`（上限 `backoff_max_ms`）后重新 spawn。
   - 若超限：标记为 `stopped`，发送通知给宿主“插件频繁崩溃，已暂停自动重启”，用户需手动恢复。
   - 注意：`crash_exit_codes` 列表中的退出码视为正常退出（不触发重启），可用于插件自身主动退出。

9. **实现资源限制**  
   - 在 spawn 时设置进程资源限制（Windows Job Object / Linux cgroups / rlimit）。
   - 内存超限：先 SIGTERM，超时 SIGKILL。
   - CPU 超限：记录告警，不强制杀（可选项）。
   - 单请求超时：在调用链路上加入 context.WithTimeout，超时后取消请求并返回错误，不杀进程。
   - stdout 行超 8MB：内核读取时检测长度，超过时切断并记录警告，返回错误给调用方，不杀进程。

---

### 第三阶段：设置 UI 开发

10. **全局默认策略页面**  
    - 在宿主设置中添加“启动与资源”分组。
    - 提供下拉选择：`全常驻` / `核心常驻+其余按需` / `全部按需`。选择后自动修改所有插件的 `load_mode`（但保留用户自定义覆盖）。
    - 开关：空闲回收（全局启用/禁用）、自动重启（全局启用/禁用）。
    - 数字输入：空闲阈值（分钟）、心跳超时（秒）、每插件内存上限（MB）、每插件 CPU 上限（%）。

11. **单插件设置页面**  
    - 在插件详情页增加“启动与资源”标签页。
    - 显示当前 manifest 默认值，用户可覆盖（但 manifest 中的 `policy.locked` 字段可锁定某些项不让改）。
    - 启动模式：单选按钮（always / 按需 / 预热 / 禁用）。
    - 触发条件（仅按需模式可见）：多选框（命令名、UI 页面、文件后缀、事件）。
    - 空闲回收：开关 + 分钟数。
    - 自动重启：开关 + 最大次数 + 退避上限。
    - 资源限制：内存、CPU、请求超时、stdout 行上限。
    - 保存后通过 `plugin.updateSettings` RPC 写入内核 store。

12. **用户覆盖持久化**  
    - 内核 `store/` 目录中为每个插件保存 `settings_overrides.json`，记录用户修改过的字段。
    - 启动时，合并 manifest 默认值 + 用户覆盖值，得到最终生效配置。

---

### 第四阶段：home / md 适配

13. **home 插件 manifest 更新**  
    - 设置 `load_mode: always`，`recycle.idle_recycle: false`，`resilience.auto_restart: true`，`limits.mem_bytes: 268435456`。
    - 确保 home 的一次性 bat/ps1 子进程不受 supervisor 管理（内核只管理插件主进程）。

14. **md 插件 manifest 更新**  
    - 设置 `load_mode: prewarm`，`recycle.idle_recycle: true`，`recycle.max_idle_ms: 300000`，`triggers` 填入 `/md` 命令、`md-index`/`md-editor` UI、`.md` 文件后缀。
    - 大文件读取走临时文件+HTTP，manifest 中 `limits.stdout_line_bytes` 保持 8MB。

15. **验证 md 懒启动流程**  
    - 确保用户点击 .md 文件或打开 md 管理页时，触发 `GetOrStart("md")`，进程在 1-2 秒内启动完毕。
    - 确保 5 分钟无操作后进程自动回收，再次操作时重新 spawn。

---

### 第五阶段：测试与文档

16. **编写单元测试**  
    - 测试 supervisor 状态机转换（registered→running→idle→stopped）。
    - 测试心跳超时踢除（mock 插件不返回 pong）。
    - 测试空闲回收（模拟无活动后自动 shutdown）。
    - 测试自动重启与退避（模拟多次崩溃）。
    - 测试懒启动并发安全（同时多个请求触发 GetOrStart）。

17. **编写集成测试**  
    - 启动内核，安装 home 和 md 插件，验证：
      - home 立即启动，md 未启动。
      - 调用 `plugin.call("md","md.list")` 触发 md 启动。
      - 等待 6 分钟，md 进程自动回收。
      - 再次调用，md 重新启动。
      - 模拟 md 进程崩溃，观察自动重启（不超过 3 次/分钟）。
      - 模拟 md 连续崩溃超过 3 次，观察状态变为 `stopped`。

18. **更新进程说明文档**  
    - 删除“全启动”、“无闲时休眠”、“不会自动重启”等过时描述。
    - 新增“分级启动”、“懒启动”、“空闲回收”、“自动重启与退避”章节。
    - 附录：manifest 新字段完整说明、状态机转换图。

---

### 第六阶段：灰度与上线

19. **逐步切换默认策略**  
    - 先在 beta 版本中将新插件默认 `load_mode` 设为 `prewarm`（非 `always`）。
    - 收集用户反馈，特别是低内存设备上的改善。
    - 稳定后将所有非核心插件默认改为 `lazy`。

20. **监控与告警**  
    - 内核暴露 metrics：各插件当前状态、启动次数、崩溃次数、回收次数、内存占用。
    - 宿主设置页显示“插件健康状况”概览，红色标记频繁崩溃的插件。