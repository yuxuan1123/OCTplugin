根据您的确认和补充，以下是基于原始需求及讨论后生成的完整需求分析，包含功能点与非功能点，并融入了您拍板的架构决策。

---

## 一、功能性需求（FR，共 10 组）

### FR-1 插件封装与元数据
- 插件以文件夹形式拷贝到指定目录即完成安装。
- 每个插件根目录必须包含描述文件（如 `plugin.yaml` 或 `plugin.json`），声明以下字段：
  - 名称、版本、类型（Python/JS/Go/Rust/Java/Ruby/C#/HTML/混合）
  - 入口文件路径（如 `main.py`、`index.js`、`server` 可执行文件）
  - 依赖声明文件列表（如 `requirements.txt`、`package.json`、`go.mod`、`Cargo.toml` 等）
  - 权限声明列表（见 FR-7）
  - 命令注册列表（命令名、参数 schema、补全提示）
  - 共享函数声明（函数名、参数 schema、返回值类型）
  - 界面贡献点（放置区域：左侧栏/右侧栏；默认大小；加载 URL 或静态文件路径）
  - 加载时机（见 FR-5）
  - 热键/快捷键声明（按键组合、触发动作、作用域：全局/应用内）

### FR-2 多语言插件类型
- **Python 插件**：提供 `.py` 文件 + `requirements.txt`（或其他依赖文件），系统自动创建独立虚拟环境。
- **JavaScript 插件**：提供 `.js` 文件 + `package.json`，系统自动执行 `npm install`（隔离 node_modules）。
- **其他主流语言插件**（Go、Rust、Java、Ruby、C# 等）：提供源码或编译好的可执行文件 + 对应依赖声明文件（`go.mod`、`Cargo.toml`、`pom.xml`、`Gemfile`、`*.csproj` 等）。系统根据声明文件自动下载依赖并编译（若为源码）或直接使用预编译二进制。
- **HTML 插件**：提供 `.html` 文件及其资源文件夹，作为独立面板嵌入。
- **混合插件**：同时包含多种类型（如 HTML 前端 + Python 后端），系统统一编排：启动后端进程，并提供前端 URL 给宿主嵌入。

### FR-3 依赖管理
- **自动识别**：扫描插件目录下的标准依赖声明文件（支持 `requirements.txt`、`package.json`、`go.mod`、`Cargo.toml`、`pom.xml`、`Gemfile`、`*.csproj` 等）。
- **用户确认**：安装前弹出依赖列表（包名、版本、来源），用户确认后才开始下载安装。
- **全局共享缓存**：同一依赖的同一版本只下载一次，缓存在宿主全局目录（如 `~/.plugin-cache/`）。不同插件的相同依赖共享硬盘缓存，但运行时隔离（每个插件拥有自己的副本或符号链接至隔离环境）。
- **隔离运行环境**：每个插件拥有独立的依赖环境（Python 的 venv、Node 的独立 node_modules、Go 的 module cache 副本等），互不干扰。
- **回滚机制**：安装失败时自动清理已下载的部分，不影响其他已安装插件。记录操作日志以便排查。

### FR-4 生命周期
- **安装**：拷贝文件夹 → 解析描述文件 → 安装依赖 → 注册插件到宿主数据库。
- **启用**：根据加载时机启动插件进程，加载界面。
- **禁用**：停止进程，但不删除文件；保留配置和依赖缓存。
- **卸载**：停止进程，删除插件文件夹及其私有环境（如 venv/node_modules），但保留全局共享缓存。
- **更新**：替换文件后重新安装依赖，保留配置和用户授权状态。
- **依赖排序启动**：若插件 A 依赖插件 B 的共享函数，则 B 必须在 A 之前启动。系统根据描述文件中的依赖关系自动计算启动顺序。

### FR-5 加载时机（用户确认：Python 懒加载升级为必须）
- 支持六种基础模式，并可组合：
  - **启动即加载**：宿主启动时立即加载（适合后台服务类）。
  - **界面完成加载**：宿主界面渲染完成后立即加载（默认）。
  - **首次点开加载**：用户第一次点击插件图标或打开面板时加载（懒加载）。
  - **按需调用加载**：其他插件或命令调用其共享函数时才加载，调用后可配置是否卸载（如空闲 30 秒后卸载）。
  - **延迟加载**：界面完成后等待指定秒数后再加载。
  - **手动加载**：用户点击“启用”后才加载，禁用后立即卸载。
- **组合示例**：“界面完成后加载，30 秒内未使用则卸载”；“首次点开加载，之后常驻内存”。
- **用户覆盖**：在设置界面中，用户可为每个插件强制指定加载时机（如强制“永不自动加载”）。
- **加载状态反馈**：显示骨架屏、加载动画或进度条；加载超时（默认 15 秒，可配置）视为失败；失败后提示用户重试或查看日志，不影响其他插件。

### FR-6 运行时隔离
- **独立进程**：每个插件运行在独立的操作系统进程中（非线程）。
- **崩溃隔离**：一个插件崩溃（段错误、OOM、死循环）不影响宿主和其他插件进程。
- **资源限制**：
  - 运行超时：可为插件设置最大执行时间（如 30 秒），超时强制终止。
  - 内存上限：通过操作系统机制（Linux cgroups、Windows Job Object、macOS rlimit）设置内存软硬限制，超出则杀死进程。
- **进程回收**：插件退出后，宿主及时回收其资源（端口、文件句柄等）。

### FR-7 权限控制
- **声明**：插件在描述文件中列出所需权限（至少包括：`file_read`、`file_write`、`network`、`execute_command`、`local_model`、`spawn_process`）。
- **首次授权**：用户首次启用插件时，弹出权限列表，用户逐项确认或一键同意。
- **运行时强制校验**：宿主内核维护权限位图，插件每次发起敏感操作前，内核拦截并校验是否已授权。未授权操作直接拒绝并记录日志。
- **动态收回**：用户在设置中可随时撤销某个插件的某项权限，撤销后即时生效。
- **权限最小化**：插件只能做它声明且用户同意的事；未声明权限的操作一律禁止。

### FR-8 通信与共享函数
- **双向通信**：
  - 宿主 ↔ 插件：支持 JSON-RPC over stdin/stdout（轻量）或 WebSocket（适合流式）。
  - 一次性调用（request-response）和持续流式输出（如实时日志、逐句翻译）。
- **插件间通信**：不直接连接，必须经过宿主中转。插件 A 调用插件 B 的共享函数时，宿主负责查找注册表并路由。
- **共享函数注册表**：
  - 插件在描述文件中声明共享函数（名称、参数 schema、返回值类型）。
  - 宿主启动时收集并维护全局注册表，支持动态增删。
  - 调用方通过宿主 API 发起调用，宿主根据函数名路由到提供方插件。
  - 提供方插件可设置白名单，拒绝来自特定插件的调用（基于插件 ID 或权限）。
  - 支持同步返回和异步流式返回（如大文件传输、实时数据流）。

### FR-9 宿主集成
- **终端命令**：
  - 插件可注册命令（如 `/translate`），命令名自动加入宿主命令面板的自动补全列表。
  - 支持参数级补全（如 `/translate --from en --to zh` 时，`--from` 后提示语言列表）。
- **热键与快捷键**：
  - 全局热键：系统级，应用不在前台也可触发（如 `Ctrl+Shift+T` 切换翻译面板）。
  - 应用内快捷键：仅在宿主窗口获得焦点时生效。
  - 冲突处理：多个插件注册相同热键时，宿主弹出冲突提示，用户选择保留哪一个；用户可在设置中修改或禁用任何热键。
- **界面布局**（三区）：
  - **上标题栏**：应用标题、全局搜索/命令输入框、窗口控制按钮。
  - **左侧栏**：插件列表、导航、文件树、命令面板。
  - **右侧栏**：属性面板、输出日志、预览、插件功能面板。
- **插件面板互通**：点击左侧栏插件 A 的条目，右侧栏可展示插件 B 的内容（通过宿主中转消息）。

### FR-10 安全机制
- **插件不可修改宿主**：宿主内核和 UI 进程受操作系统保护，插件进程无写入权限。
- **插件不可读取其他插件私有数据**：每个插件的数据目录独立（如 `data/<plugin-id>/`），宿主强制文件系统隔离。
- **敏感操作二次确认**：涉及高危操作（如执行系统命令、删除文件）时，即使已授权，也弹出确认对话框。
- **安全模式启动**：启动时按住 Shift（或设置中开启），禁用所有插件，仅加载宿主核心。
- **插件 UI 沙箱**：插件 iframe 使用 `sandbox` 属性，禁止脚本执行（除非宿主显式开放），并通过 `postMessage` 与宿主通信，不授予 `contextBridge`。

---

## 二、非功能性需求（NFR，共 8 类）

| 类别 | 核心指标 |
|------|----------|
| **NFR-1 安全强度** | 插件 UI 不得直接访问系统资源（文件、网络、进程）；沙箱边界可防御已知绕过手法（如 prototype pollution、XSS 提权）；遵循最小权限原则。 |
| **NFR-2 可靠性** | 单个插件崩溃、死循环、OOM 均被隔离并自动回收；宿主进程不因此受影响；插件状态可通过日志回溯。 |
| **NFR-3 性能** | 宿主启动时间 ≤ 3 秒（不含插件加载）；IPC 单次调用延迟 ≤ 5ms（本地）；流式输出首字节 ≤ 50ms；命令补全响应 ≤ 100ms。 |
| **NFR-4 资源占用** | 每个插件进程内存上限可由用户配置（默认 512MB）；宿主自身内存 ≤ 200MB（不含插件）；磁盘缓存目录可清理，上限可配置。 |
| **NFR-5 跨平台一致性** | 插件 UI（HTML）在 Windows/macOS/Linux 上渲染一致（使用标准浏览器引擎）；热键注册行为一致；进程管理 API 差异由宿主抽象。 |
| **NFR-6 生态友好度** | 插件开发门槛低：只需编写业务逻辑 + 描述文件；通信协议基于标准 JSON-RPC/WebSocket；多语言地位平等，不偏袒某一种。 |
| **NFR-7 可演进性** | 插件协议版本化（描述文件 schema 带版本号）；宿主升级向后兼容旧插件；依赖解析结果可复现（lockfile 机制）。 |
| **NFR-8 易用性** | 安装零配置（拷贝即用）；依赖安装前可视化展示；加载状态可感知（骨架屏/进度条）；冲突提示清晰可理解。 |

---

## 三、已拍板的架构决策（整合入上述需求）

| 决策编号 | 内容 | 体现位置 |
|----------|------|----------|
| D1 | 依赖“共享”指硬盘下载缓存，“隔离”指运行时内存隔离 | FR-3 明确区分缓存与运行环境 |
| D2 | Python 懒加载与卸载升级为必须 | FR-5 中“首次点开加载”和“按需调用加载”成为默认推荐，且支持空闲卸载 |
| D3 | Python 沙箱基于 OS 进程（非子解释器），使用 setrlimit / Job Object | FR-6 运行时隔离机制 |
| D4 | 宿主 UI 框架选用 Electron | FR-9 界面布局基于 Electron 实现 |
| D5 | 插件 UI 仅使用 postMessage，不提供 contextBridge；宿主 UI 可使用 contextBridge | FR-10 安全机制中明确 iframe 沙箱策略 |
