# 设计要点细化 · 薄弱环节决策

> 针对架构落地拆分被指出的 7 个薄弱环节，逐条给出**明确决策、数据模型、行为契约与责任方**。
> 原则：只把能拍板的拍死；暂不能拍板的给出裁决边界与到期时点。

---

## 1. 权限位图：粒度与运行时撤销契约

### 1.1 位图数据模型

位图不是一枚比特，而是 **(插件, 动作, 实体模式) → 授权** 的二维映射，持久化于内核 store。

```go
type Permission string     // 动作类别
const (
    PFileRead      Permission = "file_read"
    PFileWrite     Permission = "file_write"
    PFileDelete    Permission = "file_delete"   // 独立于 write，write 不含 delete
    PNetwork       Permission = "network"
    PExecCommand   Permission = "execute_command"
    PSpawnProcess  Permission = "spawn_process"
    PLocalModel    Permission = "local_model"
)

type Grant struct {
    Permission Permission
    Target     Pattern   // 见 1.2；空 = 全部匹配该权限的任意目标
    Source     string    // cli / dialog / preset
    GrantedAt  string    // ISO8601
    Remember   bool
}
```

### 1.2 拦截粒度（Target Pattern）

| 权限 | 目标模式 | 匹配规则（竞态安全，全部大白名单） |
|---|---|---|
| file_read / write / delete | **目录前缀** `D:/data/proj/**` 或文件路径 | 前缀匹配；外层目录授权不自动覆盖内层（`/data/proj` 不含 `/data/proj2`） |
| network | **域名 + 端口**，串行数组 | 形如 `tcp:example.com:443`、`tcp:localhost:8080`、`any`。**支持多个条目**，`localhost:8080` 与 `evil.com:443` 是两条独立授权，一个位不混淆 |
| execute_command | **命令前缀白名单** | `git`、`docker` 等；任意未列命令不自动放行，`highRisk` 走二次确认 |
| spawn_process | **可执行文件路径** | 精确路径或目录前缀 |
| local_model | **模型路径/标识** | 精确路径或模型 id 前缀 |

- **网络拦截点**：内核不代理字节流，只在请求发起前校验目标 host:port 是否命中已授权条目；命中即放行交给插件进程（D6/FR-7 的"每次敏感操作前校验位图"语义）。
- **文件拦截点**：内核暴露受限的文件代理 API（读/写/删走内核），插件经此访问才可被拦；插件不可持有 OS 级任意路径句柄。

### 1.3 运行时撤销契约（已拍板）

> **只阻止新请求，不中断进行中的操作。**

- 撤销（`perms.revoke`）对**下一次**拦截立即生效；对已获准、执行中的系列操作不强制中断。
- 理由：中断一个写到一半的文件或执行一半的命令，会造成更不可恢复的损坏。数据一致性由事务日志（第 4 节）兜底。
- 例外：`execute_command` 若撤销发生在其**尚未 fork** 的排队阶段，可直接取消；一旦 fork 完成则令其自然结束。

---

## 2. 插件间通信延迟

### 2.1 延迟预算（补入 M1 验收）

| 路径 | 预算 | 测量口径 |
|---|---|---|
| 宿主 ↔ 内核（WS 空方法） | ≤ 1ms | 同机 P50 往返 |
| 宿主 ↔ 内核（`plugin.call` 空函数） | ≤ 3ms | request→result 全链路 |
| 跨插件 A→B（经内核中转，空函数） | ≤ 5ms | A 发起→B 返回→A 收到 |
| 流式首字节 | ≤ 50ms | 触发后 EOF 前首个 frame 到达 |

### 2.2 快速通道（M3 之后的候选，不假装不存在）

- 先在 M1/M2 用 stdio 打通并吃到预算内；达标则**暂不上**快速通道，避免复杂度。
- 若跨插件 P50 > 5ms 且高频协作成为真实瓶颈，选择其一（按平台）：
  - **同机共享内存对**（Windows 文件映射 / POSIX shm）：A 写、内核转发元数据、B 读；内核仍做权限与路由校验。
  - **Unix Socket**（类 Unix）：比 stdio 少一层 PTY/传输开销。
- 触发条件：M4 阶段用真实插件跑基准，`min` 口径评估；需在架构文档"待补充决策"登记。

---

## 3. 依赖管理器：离线与进度

### 3.1 离线 / 内网兜底（补 D3/D4）

- **本地运行时预置**：允许管理员把 uv 托管 Python 3.12 的安装包/解压产物放到固定目录 `{app}/runtime/python3.12/` 或 `~/.plugin-cache/uv/`。内核**优先**从本地查找，命中则不发起任何联网。
- **uv 离线性**：所有 uv 调用设置 `UV_CACHE_DIR`、启动 `uv python install` 时若网络不可达且本地无包 → 明确报错"离线且无本地运行时"，而非黑屏假死。
- **探测顺序**：`{app}/runtime` → `~/.plugin-cache/uv` → 联网下载（带进度）。

### 3.2 进度回调（补进 deps.status 通知）

```jsonc
{ "v":1, "jsonrpc":"2.0", "method":"deps.status",
  "params": { "pluginId":"spell", "step":"downloading",   // downloading/installing/verifying
              "percent":42, "downloadedBytes":120000, "totalBytes":300000,
              "etaMs":8000, "item":"cpython-3.12.8" } }
```

- 宿主收到后渲染进度条与 ETA，禁止无响应界面。
- 首次冷启动（下载运行时）整体放宽到 ≤30s，且有百分比。

---

## 4. 崩溃恢复：数据一致性

### 4.1 孤儿进程清理（首选机制，先于快照）

- 插件一律由内核以 **Windows Job Object / Linux cgroup** 托管，且设置 **KILL_ON_JOB_CLOSE**（Job/cgroup 随持有人退出自动终止全部子进程）。
- 内核崩溃 → Job 关闭 → 插件进程自动陪葬，**无需扫描残留**。这是兜底，不是补救。

### 4.2 周期性快照 + 事务日志

```
store/
├── snapshot.json      // 每 30s：perms 位图 + 插件 enabled/loadMode + 安装状态 + 热键
├── snapshot.json.tmp  // 原子写：先写 tmp 再 rename
└── wal/               // 事务日志（append-only）
    ├── 000001.log
    └── 000002.log
```

- **快照**：重建时加载最近成功快照，恢复权限与启用状态，**避免重弹全部授权**。
- **事务日志（WAL）契约**：每个敏感操作（文件写/删、命令执行、状态变更）进入前写一条 intent，完成后追加 commit。

```jsonc
// intent
{ "tx": "t_992", "ts": "2026-09-14T22:00:00Z", "pluginId":"spell",
  "op":"file_write", "target":"D:/data/out.txt", "staged":"D:/data/.out.txt.tmp" }
// commit
{ "tx": "t_992", "ts": "2026-09-14T22:00:01Z", "status":"committed" }
```

- **重建策略**：扫描未 commit 的 intent →
  - 文件写：用**staged 临时文件 + 原子 rename** 设计，未 commit 即丢弃 staged、目标保持上次完好版本 → 自然回滚。
  - 命令执行 / 无事务语义操作：标记"进行中被打断"，不自动重放，等宿主提示用户。
  - 简单幂等操作（状态翻转）：直接重试。

---

## 5. 协议版本协商

- 握手强制交换 `major.minor`。新增 `protocol.go`（Go 侧）+ `bridge/protocol.ts`（TS 侧）。
- 规则（已拍板）：
  - **major 不同 → 拒绝连接**，返回明确版本错误（宿主/内核需同步升级，不可混跑）。
  - **minor 不同 → 允许，降级到 min(minor) 的公共能力**：双方各自持有能力清单，按交集裁剪协议特性（例如宿主不支持 `stream` 时内核降级为一次性响应）。

```jsonc
// kernel.hello 回执（补 3.1 协议版本字段）
{ "v":1, "jsonrpc":"2.0", "id":0,
  "result": { "protocol":"1.2", "clientProtocol":"1.1",   // 取交集
              "capabilities":["stream","registry","hotkey"] } }
```

- 协议结构变更（新增方法/字段）→ minor+1；破坏性变更（改语义/删方法）→ major+1。
- 插件侧：插件启动握手同样交换插件协议版本，major 不符则拒绝启用。

---

## 6. WS 连接鉴权

- 内核监听 **localhost + 随机端口**，启动时生成 **32 字节 hex token**，经其 **stdout 首个 JSON 行**传给宿主：
  ```jsonc
  { "auth": { "port": 51234, "token": "<64hex>" } }
  ```
- 宿主连上后，**首个消息必须是携带 token 的 `kernel.hello`**；token 不符或首个消息非 hello → 内核立即关闭连接并计数。
- 端口随启动随机，避免固定端口被占/被探测；token 每次启动刷新，会话内不外泄。
- 插件不走 WS（只走 stdio），不受本机制影响，无需 token。
- 补充：WS 仅限 localhost 绑定（`127.0.0.1`），不监听 `0.0.0.0`。

---

## 7. 验收标准（M1 强化）

### 7.1 冷 / 热启动分开

| 口径 | 预算 |
|---|---|
| 热启动（运行时已 cache，仅 Electron + 内核起） | ≤ 3s |
| 冷启动（含首次下载托管 Python 3.12） | ≤ 30s，且全程有 `deps.status` 百分比进度条 |

### 7.2 压力测试（补入 M1 验收）

- 场景：**5 个 Python 插件，各每秒 100 次 `plugin.call`（空函数 ping）**持续 60s。
- 通过指标：
  - 内核进程 CPU < 20%（单核折算）；
  - 全链路 P95 延迟 < 5ms，且 P99-P95 抖动 < 2ms（容器化内核进程内观测）；
  - 无连接泄漏（WS/stdio 文件句柄数平稳，无单调增长）；
  - 内存无持续增长（GC 后回落，无泄漏）。
- 观测手段：内核暴露 debug 端点或落地 `/metrics`（Prometheus 文本格式：请求计数、延迟直方图、驻留内存），宿主可勾选开启。

---

## 8. 状态同步

新增拍板决策（并入架构落地拆分决策表，见该文 §0）：

| D7 | 权限拦截粒度 = (动作, 目标模式)；运行时撤销只阻止新请求、不中断进行中的操作 |
| D8 | WS 鉴权：随机端口 + 32B hex token 经 stdout 传递；首个消息必须是带 token 的 hello |
| D9 | 协议版本 major.minor：major 不同拒绝，minor 不同降级交集 |
| D10 | 插件由 Job Object / cgroup 托管并 KILL_ON_JOB_CLOSE，内核崩溃自动清理孤儿 |
| D11 | 30s 周期快照 + WAL 事务日志，重建从最近快照恢复并据 WAL 回滚/重试 |
| D12 | Python 运行时本地预置优先于联网；依赖安装全程百分比+ETA 回调 |