# OCTplugin 架构落地拆分

> 目标：把 OCTools（PySide6 单语言插件工具箱）重构成 **Electron 外壳(TS) + Go 原生内核 + 多语言插件生态** 的通用插件宿主平台。
> 依据：[要求.md](./要求.md)（冲突时以要求.md 为准）。参考：[OCTools 项目](D:/Project/PythonProject/OCTools)。

---

## 0. 已拍板决策

| 编号 | 内容 |
|---|---|
| D1 | **内核与宿主 IPC 通道 = localhost WebSocket**（复用 JSON-RPC，双向请求 + 流式） |
| D2 | **Go 为内核语言**，TS/Electron 为宿主/UI 层，插件与其语言平等 |
| D3 | **Python 插件用内嵌 3.12**，先写死版本为 3.12，不使用/不回退系统 Python |
| D4 | **uv 配置 `python-preference = "only-managed"`**：只用 uv 托管版 Python，绝不用系统 Python |
| D5 | 宿主 UI 用 Electron；插件 UI 仅 `sandbox iframe + postMessage`，不提供 contextBridge（要求.md D4/D5） |
| D6 | 权限最小化 + 内核维护权限位图，敏感操作每次拦截校验 |
| D7 | 权限拦截粒度 = (动作, 目标模式)；运行时撤销只阻止新请求、不中断进行中的操作 |
| D8 | WS 鉴权：随机端口 + 32B hex token 经 stdout 传递；首个消息必须是带 token 的 hello |
| D9 | 协议版本 major.minor：major 不同拒绝，minor 不同降级交集 |
| D10 | 插件由 Job Object / cgroup 托管并 KILL_ON_JOB_CLOSE，内核崩溃自动清理孤儿 |
| D11 | 30s 周期快照 + WAL 事务日志，重建从快照恢复并据 WAL 回滚/重试 |
| D12 | Python 运行时本地预置优先于联网；依赖安装全程百分比+ETA 回调 |
| D13 | 跨插件经内核中转，预算 ≤5ms；不达标才启用快速通道（shm / Unix Socket） |

> 各决策的完整数据模型、行为契约与责任方，见 [设计要点细化.md](./设计要点细化.md)。

**uv 配置要求（写死，不可回退）**

```toml
# pyproject.toml / uv.toml
python-preference = "only-managed"
```

- 含义：uv 只用自己管理的 Python（managed），**绝不**回退到系统 PATH 里的 Python。
- 落地：内嵌/托管一个 Python 3.12 运行时，与 `uv python install 3.12` 对齐；所有 Python 插件的 venv 都构建在这个托管 3.12 之上。
- 任何地方检出系统 Python 都视为 bug（检测逻辑见 M2 依赖管理器）。

---

## 1. 进程与 IPC 拓扑

```
┌──────────────────────────────────────────────────────────────┐
│  Electron 宿主 (TS)  [进程 A]                                 │
│  ├── Renderer（三区布局 / 命令面板 / iframe 插件 UI 沙箱）      │
│  ├── Main（窗口 / 热键 / 权限弹窗 / 依赖确认弹窗）              │
│  └── 内核 Router Client（仅在 Main，与内核 1:1 WS 长连接）      │
│        │  localhost WebSocket (JSON-RPC, 双向请求 + 流式)       │
└────────┼───────────────────────────────────────────────────────┘
         ▼
┌──────────────────────────────────────────────────────────────┐
│  Go 内核 daemon  [进程 B]   (由 Electron Main 拉起，崩溃即重建)  │
│  ├── 进程编排器 (spawn/杀进程树/心跳/重启/资源上限)              │
│  ├── 插件注册表 + 共享函数路由（插件间通信只经这里中转）          │
│  ├── 权限位图引擎（FR-7，每次敏感调用拦截校验）                  │
│  ├── 依赖管理器（uv/npm/go/cargo/mvn，走 D1-D3 约定）           │
│  └── JSON-RPC over stdin/stdout + WebSocket 两套桥              │
└──────────┬──────────────────┬─────────────────┬────────────────┘
           │ stdin/stdout    │ stdin/stdout    │
           ▼                 ▼                 ▼
   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐
   │ 插件: Python │   │ 插件: JS    │   │ 插件: Go/Rust│...
   │ uv 托管 3.12 │   │ node_modules│   │ 编译二进制    │
   │ venv 隔离    │   │ 隔离        │   │              │
   └─────────────┘   └─────────────┘   └─────────────┘
   （每个插件独立 OS 进程；HTML/混合插件 → 内核起后端 + 把前端 URL 交给宿主嵌入）
```

**关键点**
- 两个进程即边界：插件永远不直连宿主 renderer/磁盘，只连内核；内核是敏感操作唯一入口（FR-10）。
- 插件间**不许直连**，必须经内核注册表中转（FR-8，白名单由提供方声明）。
- 内核用 localhost WebSocket 与 Electron 通信；插件用 stdin/stdout JSON-RPC 与内核通信；流式（实时日志/大文件）二者都支持。

---

## 2. OCTools → 目标 模块映射

| OCTools 现有能力 | 落地方向 | 说明 |
|---|---|---|
| [manager.py](../D:/Project/PythonProject/OCTools/services/ext_plugins/manager.py) 状态机/心跳/重启/回收 | **全部搬进 Go 内核** | 状态常量、HEARTBEAT 5s/12s、RESTART_MAX、auto_recycle 1:1 移植 |
| [ipc.py](../D:/Project/PythonProject/OCTools/services/ext_plugins/ipc.py) JSON Lines | **升级为 JSON-RPC(2.0) over stdio + WebSocket** | 保留 request/response/event 语义 |
| [installer.py](../D:/Project/PythonProject/OCTools/services/ext_plugins/installer.py) uv→pip | **改写为 Go 依赖管理器** | 按 requirements/package.json/go.mod/Cargo.toml 派发；Python 走 uv + only-managed |
| [process.py](../D:/Project/PythonProject/OCTools/services/ext_plugins/process.py) | **改写为 Go subprocess 封装** | 加 Job Object/cgroups 资源上限，逻辑从"宿主主进程侧"沉到内核 |
| [config_store.py](../D:/Project/PythonProject/OCTools/services/ext_plugins/config_store.py) | 保留概念 | 入内核数据目录，权限/状态持久化 |
| 三 UI 模式（direct/desc/window） | **重构为 FR-1 界面贡献点** | desc→iframe 控件协议；window→独立窗口；direct→移除（宿主一律经内核） |
| home 主面板（打卡/工作模式/网络加速） | **保留为第一个插件** | 做最小可验收插件；网络加速脚本经权限门执行 |
| md 插件 | 保留为第二个验证插件 | 验证 HTML/混合类插件的界面贡献点 |

---

## 3. Go 内核模块边界

```
kernel/                       (Go module，静态编译一个 daemon)
├── cmd/kerneld/main.go       入口：读环境变量/配置、监听 WS、启动编排器
├── supervisor/               进程编排 + 共享函数注册表（FR-8 声明→路由）
│   ├── manifest.go           解析 plugin.yaml/json（FR-1 全字段；含 functions/dependencies/commands/permissions 声明）
│   ├── manager.go            插件生命周期 + 共享函数表（Register/FuncList/CallFunc）
│   ├── plugin.go             stdio 双工 + gate.*/registry.call 路由（插件→内核）
│   └── reslimit.go           Windows Job Object / Linux cgroups / rlimit 统一抽象（计划）
├── perms/                    权限位图引擎
│   ├── perms.go              (动作,目标) Grant 位图 + 授权/持证(store/perms.json) + Revoke 即时生效
│   └── gate.go               每次敏感调用前拦截校验（插件 enter/exit 门请求统一走它）
├── deps/                     依赖管理器（阶段B 已落地 Python/uv；阶段F 落地 Node/npm）
│   ├── deps.go               Preview/Install/回滚 + 隔离环境(venv / 插件内 node_modules) + Config(store/deps.json) 持久化
│   └── python.go             uv venv + pip -r（仅 uv 托管 3.12, only-managed）
├── ipc/                      JSON-RPC 桥
│   ├── ws.go                 WebSocket 供宿主（宿主→内核 + 插件调度入口）
│   └── stdio.go              stdin/stdout 对接插件
└── protocol/                 协议类型、错误码、版本号(major.minor)
```

**Python 运行时约定（D3/D4）**
- 内核只认托管 Python 3.12（uv managed），地址固定，不扫系统 PATH。
- 创建插件 venv 一律 `uv venv --python 3.12 --python-preference only-managed`。
- 进程编排 spawn Python 插件时，解释器必须是该托管 3.12 的绝对路径，不查 PATH。

**Node/JS 运行时约定（阶段F）**
- Node 插件依赖隔离在插件目录内的 `node_modules`（manifest `type` 含 node/js/javascript 判定）。
- `npm install` 用随 node 分发的 `npm-cli.js`（规避 fnm/nvm 的 `.cmd` shim），并剔除父 shell 残留的 `npm_config_*` 环境变量（防 npm≥11 EALLOWSCRIPTS）。
- spawn Node 插件用 node 绝对路径跑 `manifest.entry`；解释器经 `deps.NodeInterp` 解析，进程/重启与 Python 同走 `interp()`。

---

## 4. TS 宿主模块边界

```
host/                          (Electron + React/Vue，打包进 Main/Renderer)
├── bootstrap.ts              拉起/守护 Go 内核（崩溃重启）
├── bridge/                   与内核的 WS 客户端（请求/响应/流式封装）
├── main/                     窗口、托盘、全局热键、导航
├── ui/
│   ├── shell/                上标题栏 + 左栏 + 右栏（三区，FR-9）
│   ├── command-palette/       /translate 命令 + 参数级补全
│   ├── plugin-frame/         沙箱 iframe（sandbox=… + postMessage）
│   └── panels/               打卡 / 工作模式 / 网络加速 / 今日任务（移植 home）
└── dialogs/                  首次授权、依赖确认、高危二次确认、热键冲突
```

---

## 5. 落地顺序（里程碑）

**M1 · 骨架打通**（证明"宿主↔内核↔插件"能跑）
1. Go daemon 最小版：起一个插件子进程 + JSON-RPC over stdio + 心跳。
2. Electron 拉起内核 + WS 连上 + 两侧日志互通。
3. 用 home 插件做第一个验收：面板渲染 + 网络加速脚本经权限门执行。

M1 验收（详见 [设计要点细化.md](./设计要点细化.md) §7）：
- 热启动（运行时已 cache）≤ 3s；冷启动（含下载托管 3.12）≤ 30s，且有 `deps.status` 百分比进度。
- 延迟：宿主↔内核空方法 ≤1ms、plugin.call 空函数 ≤3ms、跨插件经中转 ≤5ms、流首字节 ≤50ms。
- 压力：5 个 Python 插件各 100 call/s（空 ping）持续 60s，内核 CPU<20%、P95<5ms、无句柄/内存泄漏。

**M2 · 内核硬隔离**（安全/可靠性主体）
4. supervisor 状态机 + Job Object/cgroups 资源上限 + 崩溃重启 + auto_recycle。
5. 依赖管理器框架（先 Python venv（uv only-managed）+ npm，Go/Rust 编译随后）。
6. 权限位图 gate + 首次授权弹窗。

**M3 · 插件生态协议**
7. manifest 解析（FR-1 全字段）+ 共享函数注册表 + 插件间路由。
8. 命令面板补全 + 全局热键 + 冲突处理。
9. HTML/混合插件：iframe 沙箱 + postMessage + 宿主嵌入前端 URL。

**M4 · 多语言 + 打磨**
10. Go/Rust/Java/C# 编译链插件；依赖缓存与 lockfile；安全模式启动。
11. UI 按用户偏好打磨（白底黑字/碳灰底白、暗金按钮、打卡/工作模式/网络加速职责分离、实时状态探测、#eae1d1 代码块、逐块复制按钮）。

---

## 6. 实施进展（按阶段推进，均已冒烟验证）

> 阶段化落地记录；每条注明实际修改的文件与验证方式。

### 阶段A · 权限位图 + 插件双工（已验证）
- [perms.go](../kernel/internal/perms/perms.go)：`Check`/`Authorize`/`Revoke`/`Declare`，授权持久化到 `store/perms.json`，最小权限（只能授声明项），撤销即时生效。
- [plugin.go](../kernel/internal/supervisor/plugin.go)：插件 `enter/exit` 门请求（`gate.file_read/file_exists`）经内核拦截，未授权统一 -32005。
- [ws.go](../kernel/internal/ipc/ws.go)：宿主 `perms.authorize/revoke/list`、`plugin.call`、`plugin.details`。
- [demo_hello](ijk)manifest.json 声明 `permissions`；宿主 UI 权限卡为打勾授权/取消回收，另有"经内核读文件"演示权限门。
- 验证：未授权读文件被 -32005 拦 → 授权放行 → revoke 后再次被拒。

### 阶段B · 依赖隔离（uv / Python，已验证）
- [deps.go](../kernel/internal/deps/deps.go)：`Preview`（扫 manifest.Dependencies 读需求文件）+ `Install`（`uv venv --python 3.12 --python-preference only-managed` 到 `deps/<id>/.venv` + `uv pip install -r`）+ 失败回滚 + `Config`（源/缓存目录，持久化 `store/deps.json`）。
- [manager.go](../kernel/internal/supervisor/manager.go)：`SetVenvResolver` + `Restart`（重启后切换隔离解释器）；插件 spawn 优先用 venv 解释器。
- [main.go](../kernel/cmd/kerneld/main.go)：启动读 `store/deps.json`，环境变量 `OCTPY_INDEX`/`OCTUV_CACHE_DIR`/`OCTRUN_UV` 临时覆盖。
- 宿主：设置卡片（镜像源/缓存目录，可保存）、依赖卡片（预览/安装并重启）。
- 验证：demo_np1/demo_np2 各自 venv 锁不同 numpy（1.26.4 vs 2.2.1），证明运行环境完全隔离（FR-3）。

### 阶段C · 共享函数 + 插件间经内核中转（已验证）
- [manifest.go](../kernel/internal/supervisor/manifest.go)：声明字段 `functions`（name/method/desc）。
- [manager.go](../kernel/internal/supervisor/manager.go)：注册表 `RegisterFunctions`/`UnregisterFunctions`/`FuncList`/`CallFunc`；启动/重启注册、停止/退出注销。
- [plugin.go](../kernel/internal/supervisor/plugin.go)：插件 → 内核 `registry.call`，实现插件 A 经内核调插件 B（跨 venv 也成立，FR-8）。
- [ws.go](../kernel/internal/ipc/ws.go)：宿主 `registry.list`/`registry.call`。
- 宿主：调试面板"共享函数"卡（刷新注册表 + 勾选一键调用）。
- 验证：`registry.list` 列出全部函数；`registry.call np2.version` 按名路由返回；`demo_np1→demo_np2` 跨插件调用返回**被调方(venv2)** 的 numpy 版本，证明调用确实经内核路由到另一隔离环境。

### 阶段F · Node/JS 插件：独立进程 + npm 依赖隔离（已验证）
- [plugin.go](../kernel/internal/supervisor/plugin.go)：`interp()` 按类型解析解释器——Node 插件用 `node.exe` 跑 `manifest.entry`，Python 用 venv/托管解释器；`Start` 以解析出的二进制直接启动（Node 即 `node index.js`）。
- [manager.go](../kernel/internal/supervisor/manager.go)：`SetNodeResolver` 注入 Node 解释器解析器（`deps.Installer.NodeInterp`），`interp()` 对 Node 返回 node 路径，spawn/restart 走同一逻辑。
- [deps.go](../kernel/internal/deps/deps.go)：`installNode` 在插件目录内 `npm install`，产出局部 `node_modules`（与 venv 同效的隔离，FR-3）；用 `bundledNpmCli`（随 node 分发的 npm-cli.js）规避 Windows 上 fnm/nvm 的 `.cmd` shim；`cleanNpmEnv` 剔除父 shell 残留的 `npm_config_*`（否则 npm≥11 误判 EALLOWSCRIPTS）；`NodeModulesReady`/`NodeInterp` 供判断就绪与解析解释器。
- [demo_js](plugins/demo_js)：manifest `type=JavaScript` + `dependencies:[{manager:"npm"}]`；`index.js` 按 JSON-RPC over stdio 实现 `/today`（用 luxon 取日期），验证 npm 隔离。
- [ws.go](../kernel/internal/ipc/ws.go)：`deps.install` 对 Node 走 `NodeModulesReady` 就绪判定 + 安装后重启切换，`deps.preview` 同样纳入 Node 就绪判断。
- 验证：`plugin.call demo_js.today` 命中 `node_modules` 内 luxon（running as node v24，luxon 3.7.2），证明 Node 插件独立进程 + npm 依赖隔离生效。

### 配套 · 宿主 UI
- 国风墨韵主题（宣纸 `#f3ecdd` + 墨 `#1e1b17` + 朱砂 `#b23a30`，refer [taiji-startpage](../index)）；三区布局：左侧 `设置/调试` 导航 + 右侧内容面板。
- 应用 logo 用 `resources/logo/octpusY.svg`；图标集复制自 OCTools `resources/icons`。

---

## 7. 待补充决策（不阻塞 M1）

已解决项（移入 §0 决策表，细节见 [设计要点细化.md](./设计要点细化.md)）：
- ~~WS 的 URL/鉴权~~ → D8 随机端口 + token；断线重连与消息幂等策略 → D9 协议协商 + WAL/快照兜底。
- ~~协议版本号/兼容策略~~ → D9 major.minor。
- ~~依赖离线兜底/进度~~ → D12 本地运行时预置 + deps.status 进度。

仍待定：
- 一键分发时托管 3.12 + uv.exe 的打包方式（要求.md 路线图"一键分发"，M4 前置）。
- 跨插件快速通道（shm / Unix Socket）的取舍：M4 实测基准后再定，触发条件见细化文档 §2.2。